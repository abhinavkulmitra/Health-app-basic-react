import React from "react";
import ReactDOM from "react-dom";

import PageNav from "./app2";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <PageNav />
  </React.StrictMode>,
  rootElement
);
